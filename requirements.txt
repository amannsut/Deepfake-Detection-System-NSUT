Flask==3.0.2
librosa==0.10.1
numpy==1.26.4
scikit-learn==1.4.1.post1
joblib==1.3.2
soundfile==0.12.1
gunicorn==21.2.0
Werkzeug==3.0.1

# NOTE FOR RENDER DEPLOYMENT:
# Render must install ffmpeg for universal audio format support.
# If using Native Environment, use the provided render.yaml and Dockerfile
# or add a build script: `apt-get update && apt-get install -y ffmpeg`
